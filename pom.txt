<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Project coordinates, replace with your own -->
    <groupId>com.example</groupId>
    <artifactId>golang-binary</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging> <!-- Use 'jar' packaging for binary deployment -->

    <build>
        <plugins>
            <!-- Copy plugin to include Go binaries in the target directory -->
            <plugin>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.2.0</version>
                <executions>
                    <execution>
                        <id>copy-resources</id>
                        <!-- here, configure your phase and goals -->
                        <phase>validate</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <!-- specify the directory where your Go binaries are located -->
                            <outputDirectory>${project.build.directory}</outputDirectory>
                            <resources>
                                <resource>
                                    <!-- Path to the Go binaries -->
                                    <directory>${project.basedir}/path-to-binaries</directory>
                                    <!-- Include all files -->
                                    <includes>
                                        <include>**/*</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Maven deploy plugin configuration -->
            <plugin>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
                <configuration>
                    <!-- GitHub package repository URL -->
                    <url>https://maven.pkg.github.com/USERNAME/REPOSITORY</url>
                    <!-- Repository id, should match with server id in settings.xml -->
                    <repositoryId>github</repositoryId>
                </configuration>
            </plugin>

        </plugins>
    </build>

    <!-- Repository configuration for GitHub Packages -->
    <distributionManagement>
        <repository>
            <id>github</id> <!-- This should match the id in your settings.xml -->
            <name>GitHub OWNER Apache Maven Packages</name>
            <url>https://maven.pkg.github.com/OWNER/REPOSITORY</url>
        </repository>

        <!-- Snapshot repository if needed -->
        <!--<snapshotRepository>-->
        <!--    ...-->
        <!--</snapshotRepository>-->
    </distributionManagement>

    <!-- GitHub Packages requires authentication, so you need to add your credentials in settings.xml -->

</project>

